// Render Backend API
// const config = { backendEndpoint: "https://dreamdestination-api.onrender.com" };

// Cyclic Backend API
const config = { backendEndpoint: "https://erin-graceful-vulture.cyclic.app" };

export default config;
